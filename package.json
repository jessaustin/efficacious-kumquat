{
  "name": "cycle-shoryu",
  "version": "0.0.0",
  "scripts": {
    "start": "beefy index.coffee:index.js --live -- --transform coffeeify --extension '.coffee' --outfile index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jess Austin <jess.austin@gmail.com>",
  "license": "AGPL-3.0",
  "dependencies": {
    "@cycle/core": "*",
    "@cycle/dom": "*",
    "@cycle/history": "*",
    "@cycle/http": "*",
    "@cycle/notification": "*",
    "cycle-local-storage": "github:tylors/cycle-local-storage",
    "rx": "*"
  },
  "devDependencies": {
    "beefy": "*",
    "browserify": "*",
    "coffeeify": "*"
  }
}
