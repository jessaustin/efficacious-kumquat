{
  "name": "cycle-shoryu",
  "version": "0.0.0",
  "scripts": {
    "start": "beefy index.coffee:index.js --live -- --transform coffeeify --extension '.coffee' --outfile index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jess Austin <jess.austin@gmail.com>",
  "license": "AGPL-3.0",
  "dependencies": {
    "@cycle/core": ">=5",
    "@cycle/dom": ">=7",
    "rx": ">=4",
    "rx-combine-latest-obj": ">=1",
    "rx-injectable-observable": ">=3"
  },
  "devDependencies": {
    "beefy": ">=2.1",
    "browserify": ">=11.1",
    "coffeeify": ">=1.1"
  }
}
